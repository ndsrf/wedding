# Tasks Document

- [x] 1. Vector Prisma schema and client singleton
  - Files: `prisma/vector.prisma`, `src/lib/db/vector-prisma.ts`
  - Create separate Prisma schema for the Neon vector database with `DocumentChunk` model and `DocType` enum using `Unsupported("vector(1536)")` for the embedding column
  - Create a singleton client that connects to `VECTOR_DATABASE_URL`; export `isVectorEnabled()` guard that returns `false` when the env var is absent
  - _Leverage: `src/lib/db/prisma.ts` (PrismaPg + Pool singleton pattern)_
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 2.1, 2.2, 2.3, 2.4_
  - _Prompt: Implement the task for spec ai-search-agentic-nupcibot, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Backend Developer with Prisma and PostgreSQL expertise | Task: Create `prisma/vector.prisma` with a `DocumentChunk` model (id UUID PK, content String, embedding Unsupported("vector(1536)"), sourceName String, docType DocType enum, weddingId String?, plannerId String?, createdAt DateTime). Add generator pointing to a separate output path (`./generated/vector-client`). Then create `src/lib/db/vector-prisma.ts` cloning the PrismaPg + Pool singleton pattern from `src/lib/db/prisma.ts`, connecting to `VECTOR_DATABASE_URL`, with pool max of 5. Export `isVectorEnabled(): boolean` (returns `!!VECTOR_DATABASE_URL`) and `vectorPrisma` (null when disabled). | Restrictions: Do NOT modify `prisma/schema.prisma`. Do NOT import vectorPrisma in any file that doesn't first check `isVectorEnabled()`. Pool max must be 5. | Success: `isVectorEnabled()` returns false when env var absent; schema compiles with `npx prisma generate --schema=prisma/vector.prisma`; singleton follows same hot-reload guard as main prisma.ts. Set task to [-] in tasks.md when starting, log implementation with log-implementation tool after completion, then mark [x] when done._

- [x] 2. RagIngestionJob model in main schema
  - File: `prisma/schema.prisma` (modify)
  - Add `RagIngestionJob` model to main schema for tracking ingestion progress
  - _Leverage: existing models in `prisma/schema.prisma` for naming/field conventions_
  - _Requirements: Req 3.6 (progress indicator), Req non-functional reliability_
  - _Prompt: Implement the task for spec ai-search-agentic-nupcibot, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Backend Developer | Task: Add `RagIngestionJob` model to `prisma/schema.prisma` with fields: id String @id @default(uuid()), triggeredAt DateTime @default(now()), totalFiles Int @default(0), processed Int @default(0), failed Int @default(0), completedAt DateTime?. Run `prisma db push` with `DATABASE_URL` to apply (do not run migrate dev — use db push per project convention). | Restrictions: Only add the new model; do not alter any existing model. Use snake_case for the table map per project convention (`@@map("rag_ingestion_jobs")`). | Success: `npx prisma db push` succeeds; `prisma.ragIngestionJob.create()` compiles without TypeScript errors. Set task to [-] in tasks.md when starting, log implementation with log-implementation tool after completion, then mark [x] when done._

- [x] 3. Shared AI provider factory
  - File: `src/lib/ai/provider.ts`
  - Create a single factory module that returns a Vercel AI SDK `LanguageModel` and `EmbeddingModel` based on `AI_PROVIDER` env var
  - _Leverage: `src/lib/ai/nupcibot.ts` and `src/lib/ai/wedding-assistant.ts` (existing provider selection logic to extract)_
  - _Requirements: 7.1, 7.2, 7.3, 7.4_
  - _Prompt: Implement the task for spec ai-search-agentic-nupcibot, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Backend Developer with Vercel AI SDK expertise | Task: Create `src/lib/ai/provider.ts` exporting `getProvider(): 'openai' | 'gemini'`, `getChatModel(): LanguageModel`, and `getEmbeddingModel(): EmbeddingModel`. Use `@ai-sdk/openai` and `@ai-sdk/google` packages. For OpenAI: chat model from `OPENAI_MODEL` (default `gpt-4o-mini`), embedding model `text-embedding-3-small`. For Gemini: chat model from `GEMINI_MODEL` (default `gemini-1.5-flash`), embedding model `text-embedding-004`. `getProvider()` reads `AI_PROVIDER` env var, falling back to `openai` if `OPENAI_API_KEY` is set, else `gemini`. | Restrictions: Do NOT modify `nupcibot.ts` or `wedding-assistant.ts` in this task. The `@ai-sdk/openai` and `@ai-sdk/google` packages must be installed if absent. Export only these three functions — no provider-specific types exposed. | Success: Module compiles; calling `getChatModel()` with `AI_PROVIDER=openai` returns an OpenAI LanguageModel; calling with `AI_PROVIDER=gemini` returns a Google LanguageModel; `getEmbeddingModel()` returns the matching embedding model. Set task to [-] in tasks.md when starting, log implementation with log-implementation tool after completion, then mark [x] when done._

- [x] 4. Text chunker utility
  - File: `src/lib/ai/chunker.ts`
  - Pure function to split raw text into overlapping ~1000-character chunks
  - _Leverage: None (pure utility, no dependencies)_
  - _Requirements: 3.3_
  - _Prompt: Implement the task for spec ai-search-agentic-nupcibot, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Backend Developer | Task: Create `src/lib/ai/chunker.ts` exporting `chunkText(text: string, chunkSize = 1000, overlap = 100): string[]`. Split text by respecting word boundaries where possible (do not cut mid-word). Each chunk is at most `chunkSize` characters. Adjacent chunks share `overlap` characters at the end/start. Return empty array for empty input. | Restrictions: No external dependencies. Function must be pure (no side effects). | Success: Unit-testable pure function; `chunkText("x".repeat(2500))` produces 3 chunks; overlap is present between consecutive chunks; empty string returns []. Set task to [-] in tasks.md when starting, log implementation with log-implementation tool after completion, then mark [x] when done._

- [x] 5. Embeddings service
  - File: `src/lib/ai/embeddings.ts`
  - Generate embedding vectors for an array of text strings using the configured AI provider, batched to max 20 per request
  - _Leverage: `src/lib/ai/provider.ts` (`getEmbeddingModel()`), Vercel AI SDK `embedMany()`_
  - _Requirements: 7.4, non-functional performance (batch of 20)_
  - _Prompt: Implement the task for spec ai-search-agentic-nupcibot, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Backend Developer with Vercel AI SDK expertise | Task: Create `src/lib/ai/embeddings.ts` exporting `generateEmbeddings(texts: string[]): Promise<number[][]>`. Use Vercel AI SDK `embedMany()` with `getEmbeddingModel()` from `provider.ts`. Split input into batches of max 20, call `embedMany` for each batch sequentially, flatten results. Return array of float arrays matching input order. | Restrictions: Batch size must not exceed 20. Do NOT call the embedding API when `texts` is empty — return [] immediately. Do NOT expose provider-specific types. | Success: `generateEmbeddings(["hello"])` returns a `number[][]` with one entry; batching works for >20 inputs; empty input returns []. Set task to [-] in tasks.md when starting, log implementation with log-implementation tool after completion, then mark [x] when done._

- [x] 6. Document ingestion pipeline
  - File: `src/lib/ai/ingestion.ts`
  - Full ingestion pipeline: fetch blob → extract text → chunk → embed → upsert to vector DB; includes fan-out re-index trigger
  - _Leverage: `src/lib/ai/chunker.ts`, `src/lib/ai/embeddings.ts`, `src/lib/db/vector-prisma.ts`, `src/lib/storage/index.ts` (`getStorageProvider()`), `src/lib/db/prisma.ts` (for `RagIngestionJob`), `pdf-parse` / `mammoth` for text extraction_
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6_
  - _Prompt: Implement the task for spec ai-search-agentic-nupcibot, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Backend Developer with async pipeline expertise | Task: Create `src/lib/ai/ingestion.ts` with three exports: (1) `ingestDocument(params: { blobUrl, sourceName, docType, weddingId?, plannerId?, jobId? }): Promise<void>` — reads file via storage provider, extracts text (PDF via pdf-parse, DOCX via mammoth, plain text fallback), chunks with `chunkText()`, generates embeddings in batches, deletes existing chunks for `sourceName` then inserts new ones via `vectorPrisma.$executeRaw`. (2) `scheduleIngestion(params): Promise<void>` — wraps `ingestDocument` for use with `waitUntil`. (3) `fanOutReindex(): Promise<{ total: number }>` — lists all blobs via `VercelBlobStorageProvider.listFiles('')`, creates a `RagIngestionJob` record, enqueues each file. Install `pdf-parse` and `mammoth` if not present. Guard entire module with `isVectorEnabled()` — no-op when disabled. | Restrictions: Errors for individual files must be caught and logged per-file without halting the fan-out. Increment `RagIngestionJob.processed` or `.failed` after each file. Use `vectorPrisma.$executeRaw` for INSERT (pgvector syntax). Delete existing chunks before re-inserting (upsert by sourceName). | Success: `ingestDocument` with a PDF blob URL produces rows in `DocumentChunk`; re-ingesting the same sourceName replaces old chunks; fan-out creates a `RagIngestionJob` with correct `totalFiles`; module is a no-op when `isVectorEnabled()` is false. Set task to [-] in tasks.md when starting, log implementation with log-implementation tool after completion, then mark [x] when done._

- [x] 7. Multi-tenant retrieval service
  - File: `src/lib/ai/retrieval.ts`
  - Cosine similarity search over `DocumentChunk` with role-aware tenant scoping
  - _Leverage: `src/lib/db/vector-prisma.ts`, `src/lib/ai/embeddings.ts`_
  - _Requirements: 4.1, 4.2, 4.3, 4.4, 4.5_
  - _Prompt: Implement the task for spec ai-search-agentic-nupcibot, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Backend Developer with pgvector expertise | Task: Create `src/lib/ai/retrieval.ts` exporting `retrieveChunks(params: { query: string, weddingId?: string, plannerId?: string, role: 'wedding_admin' | 'planner', topK?: number }): Promise<RetrievedChunk[]>` where `RetrievedChunk = { content: string, sourceName: string, score: number }`. Use `generateEmbeddings([query])[0]` to embed the query. Build a `vectorPrisma.$queryRaw` using cosine distance (`<=>`) with the correct multi-tenant WHERE clause: for `wedding_admin` → `(weddingId = $weddingId OR (plannerId = $plannerId AND docType = 'WAYS_OF_WORKING') OR docType = 'SYSTEM_MANUAL')`; for `planner` → `(plannerId = $plannerId OR docType = 'SYSTEM_MANUAL')`. Return top `topK` (default 5) results. Return [] immediately when `isVectorEnabled()` is false. | Restrictions: Never return chunks outside the tenant scope. Parameterize all SQL values — no string interpolation. Do not throw when no chunks found; return []. | Success: Retrieval returns only chunks matching the tenant filter; planner query never returns another planner's chunks; empty result set returns []; disabled vector DB returns []. Set task to [-] in tasks.md when starting, log implementation with log-implementation tool after completion, then mark [x] when done._

- [x] 8. Agentic tool definitions
  - File: `src/lib/ai/tools.ts`
  - Vercel AI SDK `tool()` definitions for `search_knowledge_base`, `get_guest_list`, and `get_rsvp_status`
  - _Leverage: `src/lib/ai/retrieval.ts`, `src/lib/db/prisma.ts` (main DB for guest data), Vercel AI SDK `tool()`_
  - _Requirements: 5.1, 5.2, 5.3, 5.4, 5.5, 9.3_
  - _Prompt: Implement the task for spec ai-search-agentic-nupcibot, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Backend Developer with Vercel AI SDK tool-calling expertise | Task: Create `src/lib/ai/tools.ts` exporting `buildTools(ctx: { weddingId?: string, plannerId?: string, role: 'wedding_admin' | 'planner' }): Record<string, CoreTool>`. Define three tools using Vercel AI SDK `tool()`: (1) `search_knowledge_base` — takes `{ query: string }`, calls `retrieveChunks(ctx + query)`, returns array of `{ content, sourceName }`; (2) `get_guest_list` — no parameters, queries `prisma.family.findMany({ where: { wedding_id: ctx.weddingId } })` with RSVP status summary; (3) `get_rsvp_status` — no parameters, returns `{ total, confirmed, pending, declined, completionPct }` from family/member counts. Tools must derive weddingId/plannerId from `ctx`, never from tool call arguments (LLM cannot override). | Restrictions: Tool execute functions must not accept tenant IDs as parameters — always use `ctx`. Do not expose raw Prisma records; map to clean return shapes. Return `{ error: string }` from execute on DB failure, never throw. | Success: `buildTools({ weddingId: 'x', role: 'wedding_admin' })` returns three named tools; each tool's execute function returns the correct shape; `search_knowledge_base` calls `retrieveChunks` with the correct ctx; `get_rsvp_status` returns numeric counts. Set task to [-] in tasks.md when starting, log implementation with log-implementation tool after completion, then mark [x] when done._

- [x] 9. RAG chat orchestration service
  - File: `src/lib/ai/rag-chat.ts`
  - Agentic streaming chat using Vercel AI SDK `streamText` with tools, system prompt injection, and citation handling
  - _Leverage: `src/lib/ai/provider.ts`, `src/lib/ai/tools.ts`, `src/lib/ai/nupcibot.ts` (`ChatMessage` type, system prompt structure)_
  - _Requirements: 5.3, 5.5, 5.6, 6.1, 6.2, 6.4, 7.1, 7.2, 7.3, 8.2_
  - _Prompt: Implement the task for spec ai-search-agentic-nupcibot, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Backend Developer with Vercel AI SDK streaming expertise | Task: Create `src/lib/ai/rag-chat.ts` exporting `streamRagChat(params: { userMessage: string, history: ChatMessage[], language: string, userName?: string, weddingId?: string, plannerId?: string, role: 'wedding_admin' | 'planner' }): Promise<Response>`. Build the system prompt (reuse the language-aware structure from `nupcibot.ts`, adding: "You are a professional wedding assistant. Answer based on the provided context. Every response that uses documents MUST end with a 'References' section listing the unique filenames used. If you cannot answer, say so instead of guessing."). Sanitize `userMessage` (strip `ignore previous instructions`, `system:` patterns). Call Vercel AI SDK `streamText({ model: getChatModel(), system, messages: [...history, { role: 'user', content: sanitizedMessage }], tools: buildTools(ctx), maxSteps: 5 })`. Return `result.toDataStreamResponse()`. | Restrictions: Must return a streaming `Response` object suitable for Next.js route handlers. Do not import Next.js types. Context (weddingId/plannerId) must come from params, never from user message. | Success: Returns a `Response` with `Content-Type: text/event-stream`; tool calls are visible in the stream; the LLM produces a "References" section when `search_knowledge_base` is called; sanitization removes injection patterns. Set task to [-] in tasks.md when starting, log implementation with log-implementation tool after completion, then mark [x] when done._

- [x] 10. Admin and Planner Nupcibot chat API routes
  - Files: `src/app/api/admin/nupcibot/chat/route.ts`, `src/app/api/planner/nupcibot/chat/route.ts`
  - Streaming POST endpoints for Wedding Admin and Planner roles, delegating to `streamRagChat` (or fallback to `generateNupciBotReply` when vector DB is disabled)
  - _Leverage: `src/lib/auth/middleware.ts` (`requireRole`, `hasWeddingAccess`), `src/lib/ai/rag-chat.ts`, `src/lib/ai/nupcibot.ts` (fallback), `src/lib/db/vector-prisma.ts` (`isVectorEnabled`), Zod_
  - _Requirements: 8.1, 8.2, 9.1, 9.2, 9.3, 9.4, non-functional security_
  - _Prompt: Implement the task for spec ai-search-agentic-nupcibot, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Full-stack Developer with Next.js API routes expertise | Task: Create two POST route handlers following the project's API route pattern (see `src/app/api/admin/guests/route.ts` for reference). Both accept `{ message: string, history: ChatMessage[], language?: string }` (validated with Zod). (1) `src/app/api/admin/nupcibot/chat/route.ts` — authenticate with `requireRole('wedding_admin')`, extract `weddingId` and `plannerId` from session. If `isVectorEnabled()`, return `streamRagChat({ ..., role: 'wedding_admin' })`; else call `generateNupciBotReply` and return `NextResponse.json({ reply })`. (2) `src/app/api/planner/nupcibot/chat/route.ts` — same but `requireRole('planner')`, pass `plannerId` only (no `weddingId`). Both routes must return 401/403 on auth failure. | Restrictions: Never pass tenant IDs from the request body to the chat service — only from session. History must be capped at last 20 messages before passing to service. Language defaults to 'EN'. | Success: Unauthenticated request returns 401; wrong role returns 403; streaming response returns `text/event-stream` when vector DB enabled; fallback returns JSON when disabled. Set task to [-] in tasks.md when starting, log implementation with log-implementation tool after completion, then mark [x] when done._

- [x] 11. Master Admin re-index and status API routes
  - Files: `src/app/api/master/rag/reindex/route.ts`, `src/app/api/master/rag/status/route.ts`
  - POST endpoint to trigger fan-out re-indexing and GET endpoint to return current ingestion progress
  - _Leverage: `src/lib/auth/middleware.ts` (`requireRole`), `src/lib/ai/ingestion.ts` (`fanOutReindex`), `src/lib/db/prisma.ts` (`RagIngestionJob`)_
  - _Requirements: 3.1, 3.6_
  - _Prompt: Implement the task for spec ai-search-agentic-nupcibot, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Backend Developer | Task: Create two route handlers. (1) `POST /api/master/rag/reindex` — authenticate with `requireRole('master_admin')`, call `fanOutReindex()` using `waitUntil` so the response returns immediately, respond `{ queued: total }`. (2) `GET /api/master/rag/status` — authenticate with `requireRole('master_admin')`, query the most recent `RagIngestionJob` from main prisma DB, return `{ total, processed, failed, inProgress: !completedAt, triggeredAt }`. Return `{ inProgress: false, total: 0 }` when no job exists. | Restrictions: The POST must not await the full ingestion — use `waitUntil` (Vercel) or fire-and-forget with error logging. Both routes must return 401/403 on auth failure. | Success: POST returns `{ queued: N }` immediately; GET returns the latest job status; repeated GETs show incrementing `processed` count; returns 401 when unauthenticated. Set task to [-] in tasks.md when starting, log implementation with log-implementation tool after completion, then mark [x] when done._

- [x] 12. Document upload ingestion hook
  - Files: Modify existing document upload route(s) that call `storageProvider.uploadFile()` for wedding/planner documents
  - Add `scheduleIngestion()` call (via `waitUntil`) after successful upload, passing correct `docType`, `weddingId`, `plannerId`
  - _Leverage: `src/lib/ai/ingestion.ts` (`scheduleIngestion`, `isVectorEnabled`), `src/lib/db/vector-prisma.ts`_
  - _Requirements: 3.2_
  - _Prompt: Implement the task for spec ai-search-agentic-nupcibot, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Backend Developer | Task: Find all API routes that upload files to storage (search for `uploadFile` or `storageProvider` calls). For each: after a successful upload, if `isVectorEnabled()`, call `scheduleIngestion({ blobUrl: result.url, sourceName: result.pathname, docType: <appropriate>, weddingId, plannerId })` wrapped in `waitUntil` (or fire-and-forget). Determine `docType` from context: uploads within a wedding context → `WEDDING_DOCUMENT`; planner ways-of-working uploads → `WAYS_OF_WORKING`; master admin uploads → `SYSTEM_MANUAL`. | Restrictions: Never block the upload response waiting for ingestion. If `isVectorEnabled()` is false, skip entirely. Do not modify upload logic — only add the post-upload hook. | Success: After a wedding document upload, a `DocumentChunk` row appears in the vector DB within 30s; upload response time is unaffected; no ingestion triggered when vector DB is disabled. Set task to [-] in tasks.md when starting, log implementation with log-implementation tool after completion, then mark [x] when done._

- [x] 13. Nupcibot chat UI — streaming and citation rendering
  - File: `src/components/admin/NupcibotChat.tsx` (modify existing)
  - Replace the current fetch-based chat with Vercel AI SDK `useChat` hook for streaming; parse and render "References" sections as a distinct citation footer
  - _Leverage: Vercel AI SDK `ai/react` (`useChat`), existing `src/components/ui/` primitives, existing `NupcibotChat.tsx` component structure_
  - _Requirements: 8.1, 8.2, 8.3, 8.4, 8.5, 6.3, 6.4_
  - _Prompt: Implement the task for spec ai-search-agentic-nupcibot, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Frontend Developer with React and Vercel AI SDK expertise | Task: Modify `src/components/admin/NupcibotChat.tsx` to use `useChat` from `ai/react` pointing to `/api/admin/nupcibot/chat`. Pass `{ language, history }` as body. Render streaming messages progressively. After a message is complete, parse it: split on `\n\nReferences\n` (or `\n\n**References**\n`) — render the main body as markdown-formatted text and the references list (if present) as a visually distinct small-text block with a separator line above it (e.g. `<hr />` + `<ul className="text-xs text-muted-foreground">`). Show a pulsing dot indicator while the assistant is generating. Preserve existing mobile-friendly layout; ensure the chat does not cover bottom navigation on small screens. Install `ai` package if not already installed. | Restrictions: Do not break the existing non-streaming fallback path (when server returns JSON instead of a stream, `useChat` will still work). Do not remove existing UI structure — enhance it. Keep the component `'use client'`. | Success: Typing a message shows streaming tokens appearing progressively; a response with "References" shows a separated citation block; loading indicator visible during generation; mobile layout is unaffected. Set task to [-] in tasks.md when starting, log implementation with log-implementation tool after completion, then mark [x] when done._

- [x] 14. Master Admin re-index UI
  - File: Modify the Master Admin dashboard page (find via `src/app/master/`)
  - Add a "Re-index Documents" button and a live progress indicator that polls `/api/master/rag/status`
  - _Leverage: existing Master Admin page components, existing `src/components/ui/` primitives, `src/app/api/master/rag/status/route.ts` (task 11)_
  - _Requirements: 3.1, 3.6_
  - _Prompt: Implement the task for spec ai-search-agentic-nupcibot, first run spec-workflow-guide to get the workflow guide then implement the task: Role: Frontend Developer | Task: Find the Master Admin dashboard page under `src/app/master/`. Add a "Documents & AI" section with: (1) A "Re-index All Documents" button that POSTs to `/api/master/rag/reindex` and starts polling; (2) A progress bar showing `processed / total` files with counts; (3) A status badge (Idle / In Progress / Complete) derived from the status API response. Poll `/api/master/rag/status` every 3 seconds while `inProgress` is true; stop when `completedAt` is set. Show failure count if `failed > 0`. Only render this section when `NEXT_PUBLIC_VECTOR_ENABLED` is `"true"` (add this env var). | Restrictions: Polling must be cleaned up on component unmount. Button must be disabled while ingestion is in progress. Do not add a new page — add a section to the existing dashboard. | Success: Clicking the button triggers re-index and the progress bar updates every 3s; progress reaches 100% when ingestion completes; button re-enables after completion; no polling occurs when component unmounts. Set task to [-] in tasks.md when starting, log implementation with log-implementation tool after completion, then mark [x] when done._
